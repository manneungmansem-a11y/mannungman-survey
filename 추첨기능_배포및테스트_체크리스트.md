# 추첨(당첨확인) 기능 — 배포 및 테스트 체크리스트 (v3, 경품 수량 커스터마이즈 + 관리자 수동 확정)

## 1. 구조 요약

### 구글시트 (기존 시트는 변경 없음)
| 시트 | 용도 | 비고 |
|---|---|---|
| `responses` | 기존 설문 응답 원본 | **변경 없음 — 추첨 기능은 읽기만 함** |
| `settings` / `logs` | 기존 설정/로그 | 변경 없음 |
| `raffle_config` | 현재 공개 대상 회차(`currentRoundId`), 공개 여부(`revealEnabled`) | 신규 (자동 생성) |
| `raffle_rounds` | 회차별 조건(날짜기준/참여유형)·대상수·중복수·경품수량(총/1·2·3등/참가상)·미당첨·남는경품·상태·공개여부 | 신규 (자동 생성, v3에서 경품수량 관련 컬럼 추가 — 기존 회차 행은 유지된 채 뒤에 컬럼만 추가됨) |
| `raffle_results` | raffleId 기준 **대상자 전원**(당첨자 + 낙첨자 `tier='NONE'`)의 결과 | 신규 (자동 생성) |
| `raffle_logs` | 추첨 실행·공개 변경·당첨확인 기록 (번호는 마스킹 저장) | 신규 (자동 생성) |

- 신규 시트 4개는 raffle 관련 API가 처음 호출될 때 자동 생성됩니다. (`raffleSetupSheets()` 1회 수동 실행도 가능)
- 관리자가 "추첨 실행"을 누를 때마다 새 **회차(raffleId)** 가 생성됩니다. 예: `RAFFLE_20260707_184230`
- 당첨 판정 키는 **숫자만 남긴 휴대폰 번호**. 동일 번호 다중 제출 시, 선택한 **필터 조건 내에서** 최신 1건만 추첨 대상 풀에 포함.
- 연락처가 비어 있는 기존(레거시) 응답은 추첨 대상에서 자동 제외됩니다.
- `raffle_config.currentRoundId`가 가리키는 회차만 사용자 당첨확인(`rafflecheck`) 조회 대상입니다. 과거 회차 기록은 삭제되지 않고 `raffle_rounds`/`raffle_results`에 그대로 남습니다.

### Apps Script 액션 (전부 신규 — 기존 액션과 충돌 없음)
| 액션 | 메서드 | 인증 | 설명 |
|---|---|---|---|
| `rafflestatus` | GET | 공개 | 현재 회차 실행/공개 여부만 반환 (개인정보 없음) |
| `rafflecheck` | POST | 공개 | 휴대폰 번호로 본인 결과만 조회 + 확인시간/횟수 기록 |
| `rafflepreview` | GET/POST | 관리자 | 조건(날짜기준/참여유형)에 맞는 **후보자 상세 목록**(이름/번호/유형/응답일시/중복여부) 조회 — 자동 확정 아님 |
| `rafflerun` | POST | 관리자 | **관리자가 체크한 `selectedPhones`만** + 직접 입력한 경품 수량(`prizeTotalCount`/`tier1-3Count`)으로 새 회차 생성·추첨 실행. 현재 회차가 이미 실행됨 상태면 초기화 전까지 차단(`ALREADY_EXECUTED`) |
| `raffletogglereveal` | POST | 관리자 | 현재 회차 당첨확인 공개 on/off |
| `rafflereset` | POST | 관리자 | `confirm:'RESET'` 필수, 현재 회차만 백업 후 초기화 (과거 회차 유지) |
| `rafflerounds` | GET | 관리자 | 회차 이력 목록 |
| `rafflewinners` | GET | 관리자 | 지정 회차(기본: 현재 회차) 대상자 전원 목록 |
| `raffleduplicates` | GET | 관리자 | 지정 회차 조건 기준 중복 제출 번호 현황 |

## 2. 배포 순서

1. **구글시트 백업**: 시트에서 [파일 → 사본 만들기]로 전체 백업 (권장)
2. **Apps Script 갱신**: `apps-script.gs` 전체 내용을 Apps Script 편집기에 붙여넣기 → 저장
3. **재배포**: [배포 → 배포 관리 → 수정(연필) → 새 버전 → 배포]
   - ⚠️ "새 배포"가 아닌 **기존 배포의 새 버전**으로 해야 URL이 유지됩니다
4. `survey.html`, `admin.html` Git 커밋 & 배포
5. 아래 테스트 진행

## 3. 회귀 테스트 (기존 기능 — 반드시 먼저 확인)

- [ ] 일반 사용자 설문 제출 → 완료 화면 표시 → `responses` 시트에 새 행 정상 추가
- [ ] 임직원/파트너 비밀코드 진입 → 설문 제출 정상
- [ ] 제출된 행의 컬럼 위치가 기존 응답과 동일한지 (밀림 없음)
- [ ] 관리자 페이지 로그인 → 전체 요약 / 개별 응답 목록 / 통계 분석 / 주관식 답변 정상 표시
- [ ] 처리상태 변경 / 관리자 메모 저장 정상
- [ ] 엑셀 다운로드 정상
- [ ] 진입 코드 변경 정상

## 4. 추첨 기능 테스트

### 관리자 페이지 (추첨 관리 탭)
- [ ] "추첨 관리" 탭 진입 → 조건 선택 카드 + 경품 수량 설정 카드 + 상태 카드(현재 회차/공개여부/당첨자수/중복수) 표시
- [ ] 날짜 기준을 "특정 날짜"/"특정 기간"으로 바꾸면 날짜 입력 필드가 나타나는지
- [ ] 총 경품 수량 / 1등·2등·3등 인원을 직접 입력할 수 있고, 참가상 인원(자동계산 필드)이 `총수량-1등-2등-3등`으로 실시간 반영되는지
- [ ] "대상자 미리보기" 클릭 → 조건 조회 결과 / 중복 제출 번호 / 중복 제거 후 후보자 / 관리자 선택 인원(0명) 카드 + **후보자 목록 테이블**(체크박스/이름/번호마스킹/유형/응답일시/중복여부) 표시
- [ ] 미리보기 직후 체크박스는 **전체 미체크** 상태로 시작하는지 (설문 참여자가 자동으로 추첨 대상이 되지 않는지)
- [ ] 후보자 목록에서 개별 체크 시 "관리자 선택 인원"이 실시간으로 갱신되는지
- [ ] "전체 선택" / "전체 해제" 버튼 동작 확인
- [ ] 후보자 목록의 휴대폰번호가 `010-****-1234` 형태로 마스킹되어 있고, 클릭 시 전체 번호로 토글되는지
- [ ] 경품 수량/선택 인원을 바꿀 때마다 확률(1등/2등/3등/참가상/미당첨 명수 및 %)과 남는 경품 수가 실시간으로 재계산되는지
- [ ] 대상자 수 ≤ 경품 수량일 때 "남는 경품"이 계산되고, 대상자 수 > 경품 수량일 때 "미당첨" 인원이 계산되는지 (명세 예시 숫자로 검증)
- [ ] 1등+2등+3등 > 총 경품 수량 → "1등, 2등, 3등 인원 합계가 총 경품 수량보다 많습니다..." 배너 표시 + 추첨 실행 버튼 비활성화
- [ ] 1등+2등+3등 > 선택 인원(위 조건은 아닐 때) → "상위 당첨 인원이 추첨 대상자 수보다 많습니다..." 경고 배너 표시 (버튼은 비활성화되지 않음)
- [ ] 선택 인원 0명 → "추첨 대상자가 없습니다..." 배너 + 버튼 비활성화
- [ ] "선택한 대상자로 추첨 실행" → 확인 팝업(회차명/최종 대상 인원/경품 수량/등수별 인원/참가상/미당첨/남는경품 표시) → 실행 → 등수별 배정 결과 표시
- [ ] 체크하지 않은 후보자는 실제 추첨 대상에서 제외되는지 (예: 조회 30명 중 20명만 체크 후 실행 → `raffle_results`에 20명만 저장)
- [ ] `raffle_results` 시트에 대상자 전원(당첨자+낙첨자 `NONE`) 저장 확인, `raffle_rounds`에 새 회차 1행 + 경품수량/미당첨/남는경품 컬럼 값 추가 확인
- [ ] 현재 회차가 실행된 상태에서 "대상자 미리보기"만 다시 해도 되지만, "추첨 실행"을 다시 누르면 "이미 추첨이 완료된 회차입니다. 재추첨이 필요한 경우 초기화 후 다시 진행해주세요." 안내와 함께 차단되는지
- [ ] 위 상태에서 "현재 회차 초기화"(재추첨 입력) 후에는 새 조건/경품수량으로 다시 추첨 실행이 가능한지
- [ ] "당첨확인 공개 켜기" → 상태 카드 "공개중" 전환
- [ ] 당첨자 목록: 등수/확인여부 필터 동작, 번호 클릭 시 블러 해제, 최초/최근 확인시각·확인횟수 표시
- [ ] 중복 제출 번호 목록 표시 (동일 번호 2회 제출 테스트 데이터로 확인)
- [ ] 추첨 회차 이력 표에서 과거 회차 "보기" 클릭 → 해당 회차 당첨자/중복 목록으로 전환, "현재 회차로 돌아가기" 동작. 이력 표에 후보/최종선택/경품수량/참가상/미당첨/남는경품 컬럼이 회차별로 정확히 표시되는지
- [ ] 현재 회차 초기화: "재추첨" 입력 → 백업 시트(`raffle_results_backup_*`) 생성 + 현재 회차 결과만 삭제 + 과거 회차 기록 유지 + 공개 자동 꺼짐

### 설문 페이지 (당첨확인)
- [ ] 공개 전: 유형 선택 화면 하단 "당첨확인하기" 버튼 비활성 + "추첨 결과 공개 후 확인할 수 있습니다." 문구
- [ ] 공개 후: 새로고침 시 버튼 활성화
- [ ] 휴대폰 번호 입력이 010 / 1234 / 5678 3칸으로 분리되어 있고, 자리 채우면 자동으로 다음 칸 이동
- [ ] 백스페이스로 빈 칸에서 이전 칸으로 자연스럽게 이동
- [ ] `010-1234-5678` / `01012345678` / `010 1234 5678` 붙여넣기 시 3칸에 자동 분리되어 들어가는지
- [ ] 잘못된 형식 입력(숫자 아님/자릿수 오류) → "올바른 휴대폰 번호를 입력해주세요." 표시
- [ ] 참여 이력 없는 번호 → "입력하신 휴대폰 번호로 확인되는 설문 참여 내역이 없습니다..." 표시
- [ ] 어제 참여자만 추첨한 상태에서 오늘 참여자가 확인 → "이번 추첨 대상자가 아닙니다" 표시
- [ ] 오늘 조건 조회 결과에는 포함됐지만 관리자가 체크하지 않은 사람이 당첨확인 시도 → "설문 참여 내역은 확인되었지만, 이번 추첨 대상자가 아닙니다." 표시 (체크된 사람만 결과 확인 가능)
- [ ] 당첨 번호 입력 → 룰렛 2.6~3.5초 회전 → 해당 등수 칸에 정지 → 등수/경품 표시
- [ ] 낙첨 번호(대상자이지만 미당첨) → 룰렛 후 "아쉽지만 당첨되지 않았습니다" 표시
- [ ] 같은 번호로 재조회 → 결과 동일(재추첨 아님) + 최초 확인시각 유지, 확인횟수 증가
- [ ] `raffle_results` 시트에서 confirmed=TRUE, firstConfirmedAt/lastConfirmedAt/confirmedCount 기록 확인
- [ ] 설문 완료 화면 하단에도 당첨확인 버튼 표시 확인
- [ ] 모바일(375px 폭)에서 3분할 입력/룰렛/모달 UI 깨짐 없는지 확인
- [ ] 설문 페이지 어디에도 이름/전체 번호/전체 당첨자 명단이 노출되지 않는지 확인

### 조건별 필터 테스트
- [ ] "오늘 참여자"만 추첨 실행 → 오늘 제출한 번호만 대상자로 포함되는지
- [ ] "특정 유형"(예: 파트너)만 추첨 실행 → 해당 유형만 대상자로 포함되는지
- [ ] 동일 번호가 필터 기간 내 2회 이상 제출된 경우 1명으로 처리되고, 미리보기의 "중복 제출 번호"에 반영되는지

### 안전성 확인
- [ ] 추첨 관련 시트를 삭제해도 설문 제출은 정상 동작 (raffle 시트는 재생성됨)
- [ ] `rafflestatus` 응답에 개인정보가 없는지 확인 (실행/공개 여부, 회차명 정도만)
- [ ] `raffle_logs`에 기록되는 번호가 마스킹(`010****5678`) 형태인지 확인
- [ ] 앞자리 0이 빠진 10자리 번호(예: `1083163121`)가 있어도 `01083163121`로 보정되어 조회되는지
