# 추첨(당첨확인) 기능 — 배포 및 테스트 체크리스트

## 1. 구조 요약

### 구글시트 (기존 시트는 변경 없음)
| 시트 | 용도 | 비고 |
|---|---|---|
| `responses` | 기존 설문 응답 원본 | **변경 없음 — 추첨 기능은 읽기만 함** |
| `settings` / `logs` | 기존 설정/로그 | 변경 없음 |
| `raffle_config` | 추첨 실행 여부(`executed`), 공개 여부(`revealEnabled`), 실행 시각 | 신규 (자동 생성) |
| `raffle_results` | phone / tier / prize / drawnAt / confirmed / confirmedAt / responseId | 신규 (자동 생성) |
| `raffle_logs` | 추첨 실행·공개 변경·당첨확인 기록 (번호는 마스킹 저장) | 신규 (자동 생성) |

- 신규 시트 3개는 raffle 관련 API가 처음 호출될 때 자동 생성됩니다. (원하면 Apps Script 편집기에서 `raffleSetupSheets()` 1회 수동 실행도 가능)
- 당첨 판정 키는 **숫자만 남긴 휴대폰 번호**. 동일 번호 다중 제출 시 **최신 1건만** 추첨 풀에 포함.
- 연락처가 비어 있는 기존(레거시) 응답은 추첨 대상에서 자동 제외됩니다.

### Apps Script 액션 (전부 신규 — 기존 액션과 충돌 없음)
| 액션 | 메서드 | 인증 | 설명 |
|---|---|---|---|
| `rafflestatus` | GET | 공개 | 실행/공개 여부만 반환 (개인정보 없음) |
| `rafflecheck` | POST | 공개 | 휴대폰 번호로 본인 결과만 조회 + 확인시간 기록 |
| `rafflerun` | POST | 관리자 | 추첨 1회 실행 (재실행 차단) |
| `raffletogglereveal` | POST | 관리자 | 당첨확인 공개 on/off |
| `rafflereset` | POST | 관리자 | `confirm:'RESET'` 필수, 백업 후 초기화 |
| `rafflewinners` | GET | 관리자 | 당첨자 목록 |
| `raffleduplicates` | GET | 관리자 | 중복 제출 번호 현황 |

## 2. 배포 순서

1. **구글시트 백업**: 시트에서 [파일 → 사본 만들기]로 전체 백업 (권장)
2. **Apps Script 갱신**: `apps-script.gs` 전체 내용을 Apps Script 편집기에 붙여넣기 → 저장
3. **재배포**: [배포 → 배포 관리 → 수정(연필) → 새 버전 → 배포]
   - ⚠️ "새 배포"가 아닌 **기존 배포의 새 버전**으로 해야 URL이 유지됩니다
4. `survey.html`, `admin.html` Git 커밋 & 배포
5. 아래 테스트 진행

## 3. 회귀 테스트 (기존 기능 — 반드시 먼저 확인)

- [ ] 일반 사용자 설문 제출 → 완료 화면 표시 → `responses` 시트에 새 행 정상 추가
- [ ] 임직원/파트너 비밀코드 진입 → 설문 제출 정상
- [ ] 제출된 행의 컬럼 위치가 기존 응답과 동일한지 (밀림 없음)
- [ ] 관리자 페이지 로그인 → 전체 요약 / 개별 응답 목록 / 통계 분석 / 주관식 답변 정상 표시
- [ ] 처리상태 변경 / 관리자 메모 저장 정상
- [ ] 엑셀 다운로드 정상
- [ ] 진입 코드 변경 정상

## 4. 추첨 기능 테스트

### 관리자 페이지 (추첨 관리 탭)
- [ ] "추첨 관리" 탭 진입 → 상태 카드(미실행/비공개/대상 수/중복 수) 표시
- [ ] 추첨 실행 전 "당첨확인 공개" 버튼 비활성 확인
- [ ] "추첨 실행" → 확인 팝업(대상 인원 표시) → 실행 → 등수별 배정 결과 표시
- [ ] `raffle_results` 시트에 30명(1+3+6+20) 저장 확인 (참여자가 30명 미만이면 참여자 수만큼)
- [ ] "추첨 실행" 버튼 재클릭 시 비활성화 / 강제 호출 시 `ALREADY_EXECUTED` 확인
- [ ] "당첨확인 공개 켜기" → 상태 카드 "공개중" 전환
- [ ] 당첨자 목록: 등수/확인여부 필터 동작, 번호 클릭 시 블러 해제
- [ ] 중복 제출 번호 목록 표시 (동일 번호 2회 제출 테스트 데이터로 확인)
- [ ] 추첨 초기화: "재추첨" 입력 → 백업 시트(`raffle_results_backup_*`) 생성 + 결과 초기화 + 공개 자동 꺼짐

### 설문 페이지 (당첨확인)
- [ ] 공개 전: 유형 선택 화면 하단 "당첨확인하기" 버튼 비활성 + "추첨 결과 공개 후 확인할 수 있습니다." 문구
- [ ] 공개 후: 새로고침 시 버튼 활성화
- [ ] 잘못된 형식 입력(숫자 아님/자릿수 오류) → "올바른 휴대폰 번호를 입력해주세요." 표시
- [ ] 참여 이력 없는 번호 → "입력하신 휴대폰 번호로 확인되는 설문 참여 내역이 없습니다..." 표시
- [ ] 당첨 번호 입력 → 룰렛 2.6~3.5초 회전 → 해당 등수 칸에 정지 → 등수/경품 표시
- [ ] 하이픈 포함 입력(`010-1234-5678`)과 미포함 입력 모두 동일 결과
- [ ] 낙첨 번호 → 룰렛 후 "아쉽지만 당첨되지 않았습니다" 표시
- [ ] 같은 번호로 재조회 → 결과 동일(재추첨 아님) + `confirmedAt` 최초 시각 유지
- [ ] `raffle_results` 시트에서 confirmed=TRUE, confirmedAt 기록 확인
- [ ] 설문 완료 화면 하단에도 당첨확인 버튼 표시 확인
- [ ] 모바일(375px 폭)에서 룰렛/모달 UI 깨짐 없는지 확인
- [ ] 설문 페이지 어디에도 이름/전체 번호/전체 당첨자 명단이 노출되지 않는지 확인

### 안전성 확인
- [ ] 추첨 관련 시트를 삭제해도 설문 제출은 정상 동작 (raffle 시트는 재생성됨)
- [ ] `rafflestatus` 응답에 개인정보가 없는지 확인 (실행/공개 여부만)
- [ ] `raffle_logs`에 기록되는 번호가 마스킹(`010****5678`) 형태인지 확인

## 5. 사전 검증 완료 항목 (Node 시뮬레이션)

- 추첨 풀 중복 제거(최신 1건), 레거시(연락처 없음) 행 제외
- 등수별 인원 배정(1/3/6/20), 참여자가 경품 수보다 적을 때 순차 배정
- 재실행 차단, 비밀번호 오류 차단, confirm 없는 초기화 차단
- 공개 전 조회 차단, 미참여 번호 차단, 하이픈 입력 호환
- 당첨확인 시각 기록 및 재조회 시 유지
- 기존 doPost(설문 저장) 회귀 — 정상
